# darwin-x64 Python runtime

Place the Intel macOS Python runtime here.

Expected executable:

```text
resources/bridge-runtime/darwin-x64/python/bin/python3
```

Prepare it on macOS with:

```bash
npm run prepare:macos-python:x64
```
