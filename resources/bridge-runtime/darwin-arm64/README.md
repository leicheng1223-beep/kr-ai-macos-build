# darwin-arm64 Python runtime

Place the Apple Silicon Python runtime here.

Expected executable:

```text
resources/bridge-runtime/darwin-arm64/python/bin/python3
```

Prepare it on macOS with:

```bash
npm run prepare:macos-python:arm64
```
